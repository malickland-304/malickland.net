# Gmail account used by /api/contact to send form submissions.
# Use a Gmail app password, not the normal account password.
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Optional override for the listings API consumed by /listings.
# Leave unset to use https://malickland.net/api/listings.
LISTINGS_API_URL=
